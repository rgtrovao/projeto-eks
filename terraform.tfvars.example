# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🚀 EKS Project Configuration
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# This file contains configuration examples for the project.
# Copy to 'terraform.tfvars' and adjust as needed:
#
#   cp terraform.tfvars.example terraform.tfvars
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ┌─────────────────────────────────────────────────────────┐
# │ 📋 PROJECT IDENTIFICATION                              │
# └─────────────────────────────────────────────────────────┘

project_name = "my-project"
environment  = "dev"  # dev, staging, prod

# ┌─────────────────────────────────────────────────────────┐
# │ 🌍 AWS CONFIGURATION                                   │
# └─────────────────────────────────────────────────────────┘

aws_region = "us-east-1"

# ┌─────────────────────────────────────────────────────────┐
# │ 🌐 NETWORK CONFIGURATION                               │
# └─────────────────────────────────────────────────────────┘

# VPC CIDR (10.0.0.0/16 = 65,536 IPs)
vpc_cidr = "10.0.0.0/16"

# Availability Zones (recommended: 2 for redundancy)
availability_zones = ["us-east-1a", "us-east-1b"]

# NAT Gateway for private subnet internet access
# true  = $32/month (needed for image pulls, external APIs)
# false = $0/month  (saves money, but no internet on nodes)
enable_nat_gateway = true

# ┌─────────────────────────────────────────────────────────┐
# │ ☸️  EKS CLUSTER CONFIGURATION                          │
# └─────────────────────────────────────────────────────────┘

# Kubernetes Version
# Available versions: 1.28, 1.29, 1.30
# Recommended: Use the latest stable version
eks_cluster_version = "1.30"

# ┌─────────────────────────────────────────────────────────┐
# │ 🖥️  WORKER NODES CONFIGURATION                         │
# └─────────────────────────────────────────────────────────┘

# Capacity Type
# SPOT       = 70% cheaper, can be interrupted (ideal for dev/staging)
# ON_DEMAND  = Full price, stable (ideal for production)
eks_node_capacity_type = "SPOT"

# Instance Types
# t3.micro   = 2 vCPU, 1GB RAM    (~$0.0104/h On-Demand, ~$0.0031/h Spot)
# t3.small   = 2 vCPU, 2GB RAM    (~$0.0208/h On-Demand, ~$0.0062/h Spot)
# t3.medium  = 2 vCPU, 4GB RAM    (~$0.0416/h On-Demand, ~$0.0125/h Spot)
# t3a.micro  = AMD alternative (cheaper)
#
# 💡 Tip: List multiple types for better Spot availability:
#         ["t3.micro", "t3a.micro", "t2.micro"]
eks_node_instance_types = ["t3.micro"]

# Number of Nodes
# For studies: min=2, max=2, desired=2 (fixed)
# For production: min=3, max=10, desired=3 (auto-scaling)
eks_node_desired_size = 2
eks_node_min_size     = 2
eks_node_max_size     = 2

# Disk Size (GB)
# Recommended minimum: 20GB
# For production: 30-50GB
eks_node_disk_size = 20

# ┌─────────────────────────────────────────────────────────┐
# │ 🏷️  CUSTOM TAGS                                        │
# └─────────────────────────────────────────────────────────┘

# Additional tags for all resources
# Useful for: billing, automation, organization
tags = {
  Owner      = "your-name"
  CostCenter = "studies"
  Terraform  = "true"
  Repository = "https://github.com/your-username/eks-project"
}

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📊 COST ESTIMATE (us-east-1)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# CURRENT CONFIGURATION (Spot + NAT):
# ┌─────────────────────────────────────────────────────┐
# │ Usage 24/7:    ~$127/month                         │
# │ Usage 20h/wk:  ~$15/month  (88% savings)           │
# │ Usage 10h/wk:  ~$8/month   (94% savings)           │
# └─────────────────────────────────────────────────────┘
#
# MAXIMUM SAVINGS (Spot + NO NAT):
# enable_nat_gateway = false
# ┌─────────────────────────────────────────────────────┐
# │ Usage 24/7:    ~$95/month                          │
# │ Usage 20h/wk:  ~$12/month  (90% savings)           │
# │ Usage 10h/wk:  ~$6/month   (95% savings)           │
# └─────────────────────────────────────────────────────┘
#
# PRODUCTION (On-Demand + NAT):
# eks_node_capacity_type = "ON_DEMAND"
# ┌─────────────────────────────────────────────────────┐
# │ Usage 24/7:    ~$138/month                         │
# └─────────────────────────────────────────────────────┘
#
# 💡 See complete analysis: COSTS.md
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🎯 RECOMMENDED CONFIGURATION PROFILES
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 📚 STUDIES / LEARNING:
# ──────────────────────────────────────────────────────────
# enable_nat_gateway = false
# eks_node_capacity_type = "SPOT"
# eks_node_instance_types = ["t3.micro"]
# eks_node_desired_size = 2
# → Cost: ~$6-8/month (on-demand usage)
#
# 🧪 DEVELOPMENT / TESTING:
# ──────────────────────────────────────────────────────────
# enable_nat_gateway = true
# eks_node_capacity_type = "SPOT"
# eks_node_instance_types = ["t3.micro", "t3a.micro"]
# eks_node_desired_size = 2
# eks_node_max_size = 4
# → Cost: ~$12-15/month (on-demand usage)
#
# 🎭 STAGING / PRE-PRODUCTION:
# ──────────────────────────────────────────────────────────
# enable_nat_gateway = true
# eks_node_capacity_type = "SPOT"
# eks_node_instance_types = ["t3.small"]
# eks_node_desired_size = 2
# eks_node_max_size = 6
# → Cost: ~$140/month (24/7)
#
# 🏭 PRODUCTION:
# ──────────────────────────────────────────────────────────
# enable_nat_gateway = true
# eks_node_capacity_type = "ON_DEMAND"
# eks_node_instance_types = ["t3.small", "t3.medium"]
# eks_node_min_size = 3
# eks_node_max_size = 10
# eks_node_desired_size = 3
# → Cost: ~$180/month (24/7)
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 💡 COST-SAVING TIPS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 1. Use SPOT for non-critical environments (70% savings)
# 2. Disable NAT Gateway in dev if not needed (saves $32/month)
# 3. Destroy infra when not in use (terraform destroy)
# 4. Use t3.micro for studies (Free Tier eligible 750h/month in 1st year)
# 5. Set up cost alerts with AWS Budgets
# 6. Monitor usage with AWS Cost Explorer
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
