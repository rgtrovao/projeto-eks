# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🚀 Configuração do Projeto EKS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# Este arquivo contém exemplos de configuração para o projeto.
# Copie para 'terraform.tfvars' e ajuste conforme necessário:
#
#   cp terraform.tfvars.example terraform.tfvars
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ┌─────────────────────────────────────────────────────────┐
# │ 📋 IDENTIFICAÇÃO DO PROJETO                            │
# └─────────────────────────────────────────────────────────┘

project_name = "meu-projeto"
environment  = "dev"  # dev, staging, prod

# ┌─────────────────────────────────────────────────────────┐
# │ 🌍 CONFIGURAÇÃO AWS                                    │
# └─────────────────────────────────────────────────────────┘

aws_region = "us-east-1"

# ┌─────────────────────────────────────────────────────────┐
# │ 🌐 CONFIGURAÇÃO DE REDE                                │
# └─────────────────────────────────────────────────────────┘

# CIDR da VPC (10.0.0.0/16 = 65.536 IPs)
vpc_cidr = "10.0.0.0/16"

# Availability Zones (recomendado: 2 para redundância)
availability_zones = ["us-east-1a", "us-east-1b"]

# NAT Gateway para acesso à internet das subnets privadas
# true  = $32/mês (necessário para pull de imagens, APIs externas)
# false = $0/mês   (economiza, mas sem internet nos nodes)
enable_nat_gateway = true

# ┌─────────────────────────────────────────────────────────┐
# │ ☸️  CONFIGURAÇÃO DO CLUSTER EKS                        │
# └─────────────────────────────────────────────────────────┘

# Versão do Kubernetes
# Versões disponíveis: 1.28, 1.29, 1.30
# Recomendado: Use a versão mais recente estável
eks_cluster_version = "1.30"

# ┌─────────────────────────────────────────────────────────┐
# │ 🖥️  CONFIGURAÇÃO DOS WORKER NODES                      │
# └─────────────────────────────────────────────────────────┘

# Tipo de Capacidade
# SPOT       = 70% mais barato, pode ser interrompido (ideal para dev/staging)
# ON_DEMAND  = Preço integral, estável (ideal para produção)
eks_node_capacity_type = "SPOT"

# Tipos de Instância
# t3.micro   = 2 vCPU, 1GB RAM    (~$0.0104/h On-Demand, ~$0.0031/h Spot)
# t3.small   = 2 vCPU, 2GB RAM    (~$0.0208/h On-Demand, ~$0.0062/h Spot)
# t3.medium  = 2 vCPU, 4GB RAM    (~$0.0416/h On-Demand, ~$0.0125/h Spot)
# t3a.micro  = Alternativa AMD (mais barata)
#
# 💡 Dica: Liste múltiplos tipos para maior disponibilidade de Spot:
#          ["t3.micro", "t3a.micro", "t2.micro"]
eks_node_instance_types = ["t3.micro"]

# Número de Nodes
# Para estudos: min=2, max=2, desired=2 (fixo)
# Para produção: min=3, max=10, desired=3 (auto-scaling)
eks_node_desired_size = 2
eks_node_min_size     = 2
eks_node_max_size     = 2

# Tamanho do Disco (GB)
# Mínimo recomendado: 20GB
# Para produção: 30-50GB
eks_node_disk_size = 20

# ┌─────────────────────────────────────────────────────────┐
# │ 🏷️  TAGS PERSONALIZADAS                                │
# └─────────────────────────────────────────────────────────┘

# Tags adicionais para todos os recursos
# Útil para: billing, automação, organização
tags = {
  Owner      = "seu-nome"
  CostCenter = "estudos"
  Terraform  = "true"
  Repository = "https://github.com/seu-usuario/projeto-eks"
}

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📊 ESTIMATIVA DE CUSTOS (us-east-1)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# CONFIGURAÇÃO ATUAL (Spot + NAT):
# ┌─────────────────────────────────────────────────────┐
# │ Uso 24/7:      ~$127/mês                           │
# │ Uso 20h/sem:   ~$15/mês  (88% economia)            │
# │ Uso 10h/sem:   ~$8/mês   (94% economia)            │
# └─────────────────────────────────────────────────────┘
#
# ECONOMIA MÁXIMA (Spot + SEM NAT):
# enable_nat_gateway = false
# ┌─────────────────────────────────────────────────────┐
# │ Uso 24/7:      ~$95/mês                            │
# │ Uso 20h/sem:   ~$12/mês  (90% economia)            │
# │ Uso 10h/sem:   ~$6/mês   (95% economia)            │
# └─────────────────────────────────────────────────────┘
#
# PRODUÇÃO (On-Demand + NAT):
# eks_node_capacity_type = "ON_DEMAND"
# ┌─────────────────────────────────────────────────────┐
# │ Uso 24/7:      ~$138/mês                           │
# └─────────────────────────────────────────────────────┘
#
# 💡 Ver análise completa: CUSTOS.md
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🎯 PERFIS DE CONFIGURAÇÃO RECOMENDADOS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 📚 ESTUDOS / APRENDIZADO:
# ──────────────────────────────────────────────────────────
# enable_nat_gateway = false
# eks_node_capacity_type = "SPOT"
# eks_node_instance_types = ["t3.micro"]
# eks_node_desired_size = 2
# → Custo: ~$6-8/mês (usar sob demanda)
#
# 🧪 DESENVOLVIMENTO / TESTES:
# ──────────────────────────────────────────────────────────
# enable_nat_gateway = true
# eks_node_capacity_type = "SPOT"
# eks_node_instance_types = ["t3.micro", "t3a.micro"]
# eks_node_desired_size = 2
# eks_node_max_size = 4
# → Custo: ~$12-15/mês (usar sob demanda)
#
# 🎭 STAGING / PRÉ-PRODUÇÃO:
# ──────────────────────────────────────────────────────────
# enable_nat_gateway = true
# eks_node_capacity_type = "SPOT"
# eks_node_instance_types = ["t3.small"]
# eks_node_desired_size = 2
# eks_node_max_size = 6
# → Custo: ~$140/mês (24/7)
#
# 🏭 PRODUÇÃO:
# ──────────────────────────────────────────────────────────
# enable_nat_gateway = true
# eks_node_capacity_type = "ON_DEMAND"
# eks_node_instance_types = ["t3.small", "t3.medium"]
# eks_node_min_size = 3
# eks_node_max_size = 10
# eks_node_desired_size = 3
# → Custo: ~$180/mês (24/7)
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 💡 DICAS DE ECONOMIA
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 1. Use SPOT para ambientes não-críticos (economia de 70%)
# 2. Desabilite NAT Gateway em dev se não precisar (economiza $32/mês)
# 3. Destrua a infra quando não estiver usando (terraform destroy)
# 4. Use t3.micro para estudos (elegível Free Tier 750h/mês no 1º ano)
# 5. Configure alertas de custo no AWS Budget
# 6. Monitore uso com AWS Cost Explorer
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
